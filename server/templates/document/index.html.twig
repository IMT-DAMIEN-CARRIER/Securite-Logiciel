{% extends 'base.html.twig' %}

{% block title %}Nos incroyables documents{% endblock %}

{% block body %}
    <div class="container">
        <h1>Voici les documents de notre application.</h1>
        <p class="text-center"><strong>Ceci est une page uniquement pour les tests.</strong></p>
        <p class="text-center"><strong>Elle ne prend pas en comptes les permissions.</strong></p>
        <p class="text-center">
            <strong>
                Elle permet uniquement d'afficher de manière visuel qui est le propriétaire d'un document
                ainsi que la liste des permissions liés a ce document.
            </strong>
        </p>

        <div class="row">
            {% for document in documents %}
                {% include 'document/_document.html.twig' %}
            {% endfor %}
        </div>
    </div>
{% endblock %}
